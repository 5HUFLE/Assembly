#include<stdio.h>
#include<stdlib.h>


void ItToString(int number, char *dest)
{
    char *TempString = malloc(32*sizeof(char));
    int length = StringLength(TempString);
    StringReverse(TempString,dest,length);
}

int main(void){

    free()
    return 0;
}